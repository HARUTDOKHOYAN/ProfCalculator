<UserControl
    x:Class="ProfCalculator.Templates.Standard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProfCalculator.Templates"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="Root"
    SizeChanged="Root_SizeChanged">
    

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="oPERATOR" FontSize="20" Grid.Row="0" />
        <TextBlock Text="oPERATOR" FontSize="40" Grid.Row="1" />
        <ListView  Grid.Row="2"  ScrollViewer.VerticalScrollBarVisibility="Disabled" ItemsSource="{Binding ElementName=Root , Path=uiViewModel.buttoncontents,Mode=OneWay}" >
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid MaximumRowsOrColumns="4" Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ListView ItemContainerStyle="{StaticResource ListViewItemContainerStyle}" Height="80" Width="80" >
                        <TextBlock  Text="{Binding Content ,Mode=OneWay}" FontSize="30" Margin="40,30,0,0"/>
                    </ListView>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
